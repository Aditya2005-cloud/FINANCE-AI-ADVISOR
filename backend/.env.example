# Backend environment variables
# Add your real key in backend/.env (never commit it)
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# Optional: choose Google model
GOOGLE_MODEL=gemini-2.5-flash

# Max upload size to ingest (MB). Oversized CSV/TXT will be sampled to this size.
MAX_UPLOAD_MB=20

